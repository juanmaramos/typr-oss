{{ userRequest }}

**CRITICAL REQUIREMENTS:**

- You MUST return the COMPLETE document as valid HTML with your edits applied
- NEVER return just a message, question, explanation, or code fence
- Execute the EXACT change requested - be surgical, not comprehensive
- **STYLE MIRRORING**: Match the existing document's writing style (vocabulary, sentence structure, formality, tone) exactly
- For additions: Insert new HTML precisely where it belongs (beginning, end, specific section)
- For modifications: Change ONLY what's mentioned, preserve everything else exactly
- Maintain identical HTML structure for unchanged content
- Do NOT paraphrase, reflow, or "improve" content not specifically requested

**Current HTML Document:**
{{ currentDocument }}

**Return the COMPLETE edited HTML document now (only HTML, no explanations or markdown):**
